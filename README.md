This project is a simple website for crossfit boxes.

1. It doesn't require a webserver to work.
2. Has a yaml interface to content.

To setup run:

    npm install

To use it edit the content.yml file and run:

    wach "node compile.js"
